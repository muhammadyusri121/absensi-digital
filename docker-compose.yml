version: '3.8'

services:
  app:
    build:
    command: mix phx.server
    ports:
      - "4000:4000"
    volumes:
      - ".":/app"
    depends_on:
      - db
    environment:
      - DATABASE_URL=ecto://postgres:postgres@db:5432/absensi_digital_dev